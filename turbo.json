{
    "$schema": "https://turbo.build/schema.json",
    "pipeline": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": [..., "dist/**", ".vercel/**"]
        },
        "test": {},
        "dev": {
            "cache": false
        }
    }
}
